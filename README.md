# 轻任务模板

一个离线可用的模板化项目任务清单轻 App。

## 使用方式

1. 在模板页创建项目模板，配置子任务、占比和默认天数。
2. 在项目页选择模板新建项目。
3. 执行时勾选子任务，应用会自动计算进度并显示提前或延迟状态。
4. 在备份页导出 JSON，避免浏览器缓存清理后数据丢失。

## GitHub Pages

将本目录文件发布到仓库根目录，然后在 GitHub 仓库的 `Settings > Pages` 中选择 `Deploy from a branch`，分支选择 `main`，目录选择 `/root`。
